{
  lista: [ <% list.forEach(function(item) { %>'<%= item %>', <% }); %> ],

  listaObjetos: [ { <% objectList.forEach(function(item) { %>
    a: '<%= item.a %>',
    b: '<%= item.b %>',
    c: '<%= item.c %>',<% }); %>
    },
  ]
}
